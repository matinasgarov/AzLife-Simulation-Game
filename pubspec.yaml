name: az_life
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  audioplayers: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/json/decisions.json
    - assets/json/school_questions.json
    - assets/json/university_exams.json
    - assets/json/relationship_events.json
    - assets/gameImages/
    - assets/sounds/
